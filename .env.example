# Environment Configuration
LM_STUDIO_BASE_URL=http://localhost:1234/v1  # LM Studio API endpoint
LM_STUDIO_API_KEY=not-needed  # LM Studio doesn't require an API key in local mode
TEMPERATURE=0.7  # Model temperature for responses
MAX_TOKENS=2000  # Maximum tokens per response
